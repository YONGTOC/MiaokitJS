import { Shape } from '@antv/g';
interface DigitsAbbreviateCfg {
    unit?: 'k' | 'm' | 'b' | 't' | 'auto';
    formatter?: (val: number) => string;
    decimal?: number;
}
export default function digitsAbbreviate(shape: Shape, option: DigitsAbbreviateCfg, index: any, cfg: any): void;
export {};
