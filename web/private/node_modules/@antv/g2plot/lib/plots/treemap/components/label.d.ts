import { Shape } from '@antv/g';
import { ElementLabels } from '@antv/g2';
export declare class TreemapLabels extends ElementLabels {
    showLabels(points: any, shapes: Shape[]): void;
    getShape(shapeId: any, shapes: any): any;
    private leafText;
}
