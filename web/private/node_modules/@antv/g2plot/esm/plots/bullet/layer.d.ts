import { LayerConfig } from '../../base/layer';
import BaseBulletLayer, { BaseBulletViewConfig } from './base-layer';
export interface BulletViewConfig extends BaseBulletViewConfig {
    value: number;
}
export interface BulletLayerConfig extends BulletViewConfig, LayerConfig {
}
export default class BulletLayer extends BaseBulletLayer<BulletLayerConfig> {
    bullet: any;
    type: string;
    static getDefaultOptions(): Partial<BulletLayerConfig>;
    /** @override */
    protected drawBullets(): void;
    /** @override */
    protected adjustYAxisOptions(options: any): void;
    /** @override */
    protected adjustDataOptions(options: any): void;
}
