import EventEmitter from '@antv/event-emitter';
import { GuideCfg } from './interface';
export declare abstract class Guide<T extends GuideCfg> extends EventEmitter {
    cfg: T;
    destroyed: boolean;
    constructor(cfg: T);
    get<K extends keyof T>(name: K): T[K] | any;
    set<K extends keyof T>(name: K, value: T[K]): this;
    destroy(): void;
}
export { GuideCfg };
export default Guide;
