import Tooltip from './base';
import { TooltipCfg, ToolTipContentItem } from './interface';
export default class HtmlTooltip extends Tooltip<TooltipCfg> {
    constructor(cfg: TooltipCfg);
    _init_(): void;
    render(): void;
    _renderTpl(): void;
    clear(): void;
    show(): void;
    hide(): void;
    destroy(): void;
    _addItem(item: ToolTipContentItem, index: number): any;
    _getHtmlContent(): any;
    setPosition(oldx: number, oldy: number, target?: any): void;
}
