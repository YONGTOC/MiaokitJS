import * as G from '@antv/g';
import Tooltip from './base';
import { TooltipCfg, ToolTipContentItem } from './interface';
export default class CanvasTooltip extends Tooltip<TooltipCfg> {
    constructor(cfg: TooltipCfg);
    _init_(): void;
    render(): void;
    clear(): void;
    show(): void;
    hide(): void;
    destroy(): void;
    setPosition(oldx: number, oldy: number, target?: any): void;
    _addItem(item: ToolTipContentItem): G.Group;
    _alignToRight(width: number): void;
}
