import { TooltipCfg, ToolTipContentItem } from './interface';
import Guide from '../base';
export default class Tooltip<T extends TooltipCfg = TooltipCfg> extends Guide<T> {
    protected style?: any;
    constructor(cfg: T);
    isContentChange(title: string, items: ToolTipContentItem[]): boolean;
    setContent(title: string, items: ToolTipContentItem[]): this;
    setPosition(x: number, y: number, target?: Element): void;
    setMarkers(markerItems: ToolTipContentItem[], markerCfg: any): void;
    clearMarkers(): void;
    render(): void;
    clear(): void;
    show(): void;
    hide(): void;
}
