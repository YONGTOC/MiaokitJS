import { BBox, Canvas } from '@antv/g';
import Guide from '../base';
import { Attrs } from '../interface';
import { TooltipCfg, ToolTipContentItem } from './interface';
interface CrosshairCfg extends TooltipCfg {
    rectStyle?: object;
}
export default class Crosshair extends Guide<CrosshairCfg> {
    constructor(cfg: CrosshairCfg);
    _init_(): void;
    _addLineShape(attrs: Attrs, type: string): any;
    _renderHorizontalLine(canvas: any, panelRange: any): void;
    _renderVerticalLine(canvas: any, panelRange: any): void;
    _renderBackground(canvas: Canvas, panelRange: BBox): any;
    _updateRectShape(items: any[]): void;
    render(): void;
    show(): void;
    hide(): void;
    clear(): void;
    destroy(): void;
    setPosition(x: number, y: number, items?: ToolTipContentItem[]): void;
}
export {};
