"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var util_1 = require("@antv/util");
var bbox_1 = require("../../core/bbox");
function at(p1, p2, t) {
    return (p2 - p1) * t + p1;
}
exports.at = at;
function pointDistance(x1, y1, x2, y2, x, y) {
    var d = [x2 - x1, y2 - y1];
    if (util_1.vec2.exactEquals(d, [0, 0])) {
        return NaN;
    }
    var u = [-d[1], d[0]];
    util_1.vec2.normalize(u, u);
    var a = [x - x1, y - y1];
    return Math.abs(util_1.vec2.dot(a, u));
}
exports.pointDistance = pointDistance;
function box(x1, y1, x2, y2, lineWidth) {
    var halfWidth = lineWidth / 2;
    return bbox_1.default.fromRange(Math.min(x1, x2) - halfWidth, Math.min(y1, y2) - halfWidth, Math.max(x1, x2) + halfWidth, Math.max(y1, y2) + halfWidth);
}
exports.box = box;
function len(x1, y1, x2, y2) {
    return Math.sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
}
exports.len = len;
//# sourceMappingURL=line.js.map