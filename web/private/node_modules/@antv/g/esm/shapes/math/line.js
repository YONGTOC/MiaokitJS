import { vec2 } from '@antv/util';
import BBox from '../../core/bbox';
export function at(p1, p2, t) {
    return (p2 - p1) * t + p1;
}
export function pointDistance(x1, y1, x2, y2, x, y) {
    var d = [x2 - x1, y2 - y1];
    if (vec2.exactEquals(d, [0, 0])) {
        return NaN;
    }
    var u = [-d[1], d[0]];
    vec2.normalize(u, u);
    var a = [x - x1, y - y1];
    return Math.abs(vec2.dot(a, u));
}
export function box(x1, y1, x2, y2, lineWidth) {
    var halfWidth = lineWidth / 2;
    return BBox.fromRange(Math.min(x1, x2) - halfWidth, Math.min(y1, y2) - halfWidth, Math.max(x1, x2) + halfWidth, Math.max(y1, y2) + halfWidth);
}
export function len(x1, y1, x2, y2) {
    return Math.sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
}
//# sourceMappingURL=line.js.map