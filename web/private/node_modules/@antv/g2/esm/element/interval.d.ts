import Element from './base';
import { DataPointType } from '../interface';
export default class Interval extends Element {
    constructor(cfg: DataPointType);
    createShapePointsCfg(obj: any): DataPointType;
    isShareTooltip(): boolean;
}
