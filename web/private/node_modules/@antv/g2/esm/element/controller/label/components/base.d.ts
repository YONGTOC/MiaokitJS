import { Group } from '@antv/g';
import { DataPointType } from '../../../../interface';
export declare type ElementLabelsConstructor = new (cfg: any) => ElementLabels;
export default class ElementLabels extends Group {
    constructor(cfg: any);
    _renderUI(): void;
    destroy(): void;
    showLabels(points: any, originShapes: any): void;
    adjustItems(items: any, shapes?: any): any;
    /**
     * drawing lines to labels
     * @param  {Array} items labels
     * @param  {Object} labelLine configuration for label lines
     */
    drawLines(items: any): void;
    lineToLabel(point?: any): void;
    setLabelPosition(label?: any, point?: any, index?: any, position?: any): void;
    transLabelPoint(point: any): void;
    getOffsetVector(point: any): any;
    getDefaultOffset(point: any): number;
    getLabelOffset(point: any, index: any, total: any): {
        x: number;
        y: number;
    };
    /**
     * @protected
     * 获取labels
     * @param {Array} points points
     * @param {Array} shapes shapes
     * @return {Array} label items
     */
    protected getLabelsItems(points: any, shapes: any): any[];
    /**
     * @protected
     * 获取文本的位置信息
     * @param {Array} labelOptions labels
     * @param {Object} point point
     * @param {Number} index index
     * @return {Object} point
     */
    protected getLabelPoint(labelOptions: DataPointType, point: any, index: any): DataPointType;
    protected getLabelAlign(point: any, index: any, total: any): string;
    private _getLabelValue;
    private _getlabelOptions;
}
