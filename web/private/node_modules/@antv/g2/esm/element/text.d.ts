import Point from './point';
import { DataPointType } from '../interface';
interface ContentCfg {
    fields: string[];
    callback?: Function;
}
declare type ContentCallback = (...args: any[]) => string;
/**
 * Text 类型的几何图形
 */
export default class Text extends Point {
    constructor(cfg: DataPointType);
    /**
     * Element-Text特有的图形属性映射：内容
     * @param cfg Text文本内容的映射规则
     */
    content(field: ContentCfg): Text;
    content(field: string): Text;
    content(field: string, callback: ContentCallback): Text;
    private _getContent;
    /**
     * @override
     * @desc 添加content内容,复写cfg.text
     */
    getDrawCfg(obj: any): import("../interface").ShapeDrawCFG;
}
export {};
