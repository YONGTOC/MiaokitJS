import { DataPointType } from '../interface';
/**
 * 获取对应的主题
 * @param type
 */
export declare const getTheme: (type: string) => DataPointType;
/**
 * 注册主题
 * @param type
 * @param theme
 */
export declare const registerTheme: (type: string, theme: DataPointType) => void;
