import { Tooltip } from '@antv/component';
import { DataPointType } from '../../interface';
import View from '../view';
export default class TooltipController {
    view: View;
    theme: DataPointType;
    options: DataPointType;
    tooltip: Tooltip.Html | Tooltip.Canvas;
    private timeStamp;
    private prePoint;
    constructor(cfg: any);
    renderTooltip(): void;
    _normalizeEvent(event: any): any;
    hideTooltip(): void;
    onMouseMove: (ev: any) => void;
    onMouseOut: (ev: any) => void;
    showTooltip(point: any, view: any, target: any): void;
    clear(): void;
    private _getCanvas;
    private _getTriggerEvent;
    private _getDefaultTooltipOptions;
    private _bindEvent;
    private _offEvent;
    private _setTooltip;
    private _getItemMarker;
}
