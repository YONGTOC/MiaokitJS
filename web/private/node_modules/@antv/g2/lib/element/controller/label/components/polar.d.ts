import ElementLabels from './base';
import { DataPointType, PointObject } from '../../../../interface';
export default class PolarElementLabels extends ElementLabels {
    /**
     * @protected
     * 获取文本的位置信息
     * @param {Array} label labels
     * @param {Object} point point
     * @param {Number} index index
     * @return {Object} point
     */
    getLabelPoint(label: any, point: any, i: any): DataPointType;
    getCirclePoint(originAngle: any, offset: any, point: any, isLabelEmit: any): DataPointType;
    getArcPoint(point: PointObject, i: any): any;
    getPointAngle(point: any): number;
    /**
     * @protected
     * 获取文本旋转的方向
     * @param {Number} angle angle
     * @return {Number} angle
     */
    getLabelRotate(angle: any, offset: any, isLabelEmit: any): number;
    getLabelAlign(point: any): any;
    lineToLabel(point?: any): void;
    private _getMiddlePoint;
    private _isToMiddle;
}
