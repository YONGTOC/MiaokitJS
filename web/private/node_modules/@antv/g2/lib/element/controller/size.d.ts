import Element from '../base';
export default class SizeController {
    readonly element: Element;
    private _defaultSize;
    constructor(element: any);
    /**
     * 获取数据对应的实际画布大小
     * @param record 图形数据
     */
    getSize(record: any): any;
    /**
     * 获取数据对应归一化后的 size 值
     * @param record 图形数据
     */
    getNormalizedSize(record: any): number;
    clear(): void;
    private _getSizeValue;
    private _getDodgeCount;
    private _getDefaultSize;
    private _getCoordinateWidth;
    private _toCoordinateSize;
    private _toNormalizedSize;
}
