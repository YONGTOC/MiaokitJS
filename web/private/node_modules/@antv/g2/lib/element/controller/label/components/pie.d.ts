import { DataPointType } from '../../../../interface';
import PolarElementLabels from './polar';
export default class PieElementLabels extends PolarElementLabels {
    constructor(cfg: any);
    getDefaultOffset(point: any): any;
    /**
     * @protected
     * to avoid overlapping
     * @param {Array} items labels to be placed
     * @return {Array} items
     */
    adjustItems(originItems: any): any;
    lineToLabel(label: any): void;
    /**
     * @protected
     * get rotation for label
     * @param {Number} angle angle
     * @param {Number} offset offset
     * @return {Number} rotate
     */
    getLabelRotate(angle: any, offset: any, isLabelLimit: any): number;
    /**
     * @protected
     * get text align for label
     * @param {Object} point point
     * @return {String} align
     */
    getLabelAlign(point: any): any;
    getArcPoint(point: any): any;
    getPointAngle(point: any): any;
    getCirclePoint(angle: any, offset: any, p?: any): DataPointType;
    private _distribute;
}
