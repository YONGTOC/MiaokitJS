/**
 * @description Element 上 tooltip 的辅助类
 */
import Element from '../base';
import { PointObject, DataPointType } from '../../interface';
export default class TooltipController {
    readonly element: Element;
    private _xDistance;
    constructor(element: any);
    /**
     * 根据画布坐标在 source 中查找匹配的数据
     * @param point 画布坐标
     * @param source 数据源(已排序)
     */
    findPoint(point: PointObject, source: DataPointType[]): any;
    /**
     * 根据数据获取对应的 tooltip items 信息
     * @param data 数据记录
     * @param titleField tooltip title 字段名
     */
    getTooltipItems(data: DataPointType, titleField: string): any[];
    clear(): void;
    private _getXValueByPoint;
    private _filterYValue;
    private _getXDistance;
    private _getTooltipTitleScale;
    private _getTooltipTitle;
    private _getTooltipValueScale;
    private _getTooltipValue;
    private _getTooltipName;
    private _getSize;
}
