import { DataPointType } from '../interface';
import View from './view';
export default class Plot extends View {
    constructor(cfg: DataPointType);
    init(): void;
    /**
     * 修改画布的大小，用于外部做自适应
     * @param width
     * @param height
     * @param autoRender
     */
    changeSize(width: number, height: number, autoRender?: boolean): this;
    render(stopDrawing?: boolean): this;
    destroy(): void;
    private _initCanvas;
    private _initEvents;
    private _initForceFitEvent;
    /**
     * 自适应宽度
     * @chainable
     * @return {Chart} 图表对象
     */
    private forceFit;
    private _getContainerDOM;
}
