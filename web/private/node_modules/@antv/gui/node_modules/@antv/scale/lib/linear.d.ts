import Base, { ScaleConfig, ScaleType } from './base';
export default class Linear extends Base {
    type: ScaleType;
    isLinear: Base['isLinear'];
    values: ScaleConfig['values'];
    min?: ScaleConfig['min'];
    max?: ScaleConfig['max'];
    minLimit?: ScaleConfig['minLimit'];
    maxLimit?: ScaleConfig['maxLimit'];
    tickCount?: ScaleConfig['tickCount'];
    maxTickCount?: ScaleConfig['maxTickCount'];
    minTickInterval?: ScaleConfig['minTickInterval'];
    nice?: ScaleConfig['nice'];
    algoParam: ScaleConfig['algoParam'];
    scale(_value: number): number;
    invert(scaled: number): number;
    protected _initDefaultCfg(): void;
    protected _init(): void;
    protected _setDomain(): void;
    protected _transform(v: number): number;
    protected _setTicks(): number[];
    private _getAlgoParams;
}
