import Base, { ScaleConfig, ScaleType } from './base';
export default class Category extends Base {
    type: ScaleType;
    isCategory: Base['isCategory'];
    tickInterval: ScaleConfig['tickInterval'];
    tickCount: ScaleConfig['tickCount'];
    translate(value: any): number;
    scale(value: any): number;
    invert(scaledValue: number): any;
    getText(value: any, ...args: any[]): string;
    /**
     * ggplot将全数值的定义域视作线性
     * 再三考虑后，分类型scale还是不再支持这种兼容
     */
    protected _initDefaultCfg(): void;
    protected _init(): void;
    protected _setTicks(): any[];
}
