import { Group, Shape } from '@antv/g';
import { PointObject, ScrollBarCfg, ScrollBarTheme } from './interface';
export default class ScrollBar extends Group {
    isHorizontal: boolean;
    trackLen: number;
    thumbLen: number;
    position: PointObject;
    minThumbLen: number;
    thumbOffset: number;
    theme: ScrollBarTheme;
    scrollBarGroup: Group;
    trackShape: Shape;
    thumbShape: Shape;
    private _startPos;
    private _isMobile;
    private _clearEvents;
    constructor(scrollBarCfg: ScrollBarCfg);
    /**
     * 当前滑块滑动的位置 0 ~ 1
     */
    current(): number;
    /**
     * 更新滑道长度
     * @param newTrackLen 新的滑块长度
     */
    updateTrackLen(newTrackLen: number): void;
    /**
     * 更新滑块长度
     * @param newThumbLen 新的滑道长度
     */
    updateThumbLen(newThumbLen: number): void;
    /**
     * 更新滑块的 offset 值
     * @param offset
     */
    updateThumbOffset(offset: number): void;
    /**
     * 更新滑道位置
     * @param newPos 新的滑块位置
     */
    updateScrollBarPos(newPos: PointObject): void;
    _renderNewScrollBar(): void;
    updateTheme(theme: ScrollBarTheme): void;
    private _initScrollBar;
    private _createScrollBarGroup;
    private _createTrackShape;
    private _createThumbShape;
    private _bindEvents;
    private _onStartEvent;
    private _bindLaterEvent;
    private _onTrackClick;
    private _onMouseMove;
    private _onMouseUp;
    private _onTrackMouseOver;
    private _onTrackMouseOut;
    private _validateRange;
}
