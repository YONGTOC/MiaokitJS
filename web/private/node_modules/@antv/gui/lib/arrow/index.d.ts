import { Group, PointType } from '@antv/g';
interface ArrowCfg {
    position: PointType;
    width?: number;
    height?: number;
    direction?: string;
    shapeAttrs?: any;
}
export default class Arrow extends Group {
    private position;
    private width;
    private height;
    private direction;
    private shapeAttrs;
    constructor(cfg: ArrowCfg);
    private _init;
}
export {};
